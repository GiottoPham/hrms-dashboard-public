export type City = {
  province_id: number
  province_name: string
}
export type District = {
  district_id: number
  district_name: string
}
export type Ward = {
  ward_id: number
  ward_name: string
}
